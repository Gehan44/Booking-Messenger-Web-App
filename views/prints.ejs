<!DOCTYPE html>
<html>
<head>
  <title>Messenger - Pine Wealth Solution</title>
  <link rel="icon" href="/image/logo.png" type="image/x-icon">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel='stylesheet' href='https://fonts.googleapis.com/css?family=Roboto'>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style>
    html,
    body,
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      font-family: "Roboto", sans-serif
    }

    body {
      background-color: white;
    }

    .printButton {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
    }

    .printButton:hover {
      background-color: #0056b3;
    }

    .w3-container-sign {
      display: inline-block;
      border: 2px solid #000;
      padding: 10px;
    }

    .align-right {
      float: right;
    }

    .w3-container hr {
      margin-top: 50px;
      margin-bottom: 20px;
    }

    @media print {
      .noPrint {
        display: none;
      }
    }
  </style>
</head>

<body class="w3-light-grey">
  <div class="w3-content w3-margin-top" style="max-width:1200px;">
    <div class="w2-row-padding">
      <% if (Array.isArray(createdTracks) && createdTracks.length > 0) { %>
        <% createdTracks.forEach(track => { %>
      <div class="w3-third">
        <div class="w3-container">
          <div class="w3-container">
            <% if (track.qrImageData) { %>
              <img src="data:image/png;base64, <%= track.qrImageData %>" alt="QR Code" />
            <% } else { %>
              <img class="d-block mx-auto mb-4" src="/image/logol.png" alt="" width="180" height="170">
            <% } %>             
          </div>
          <div class="w3-container">

            <h5><b>
                <%= track.docID %>
              </b></h5>
            <h5>วันที่สั่งงาน <b>
                <%= track.createdDateTime.toLocaleString('en-GB', { day: 'numeric' , month: 'numeric' ,
                  year: 'numeric' , hour: '2-digit' , minute: '2-digit' , timeZone: 'UTC' }) %>
              </b></h5>
            <h5>วันที่นัด&nbsp;&nbsp;&nbsp; <b>
                <%= track.requestDate.toLocaleString('en-GB', { day: 'numeric' , month: 'numeric' ,
                  year: 'numeric' , timeZone: 'UTC' }) %>
              </b></h5>
            <h5>
              <%= track.docRound %>
                <% if (track.docTime !==null) { %>
                  สะดวกเวลา <%= track.docTime %>
                    <% } %>
                      </p>
                      <h4>
                      <b>
                        <%= track.docSendReturn %>
                      </b>
                      </h4>
                      <h5>
                      <%= track.docType %>
                        <%= track.docIs %>
                      </h5>
          </div>
        </div><br>
      </div>
      <div class="w3-twothird">

        <div class="w3-container w3-card w3-white w3-margin-bottom">
          <img class="d-block mx-auto mb-4" src="/image/logowithl.jpg" alt="" width="700" height="90">
          <div class="w3-container">
            <h4>ชื่อลูกค้า/ผู้ติดต่อ&nbsp;&nbsp;<b>
                <%=track.cusName%>
              </b></h4>
            <h4>โทร.ติดต่อ&nbsp;&nbsp;&nbsp;&nbsp;<b>
                <%=track.cusTel%>
              </b></h4>
            <h4>สถานที่&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<b>
                <%=track.cusPlace %>
              </b></h4>
          </div>
          <br>
          <br>
            <div class="w3-container" style="display: inline-block; vertical-align: top;">
              <p>ผู้ฝากงาน &nbsp;<%=track.dispName%>
              </p>
              <p>เบอร์โทร &nbsp;<%=track.dispTel%>
              </p>
            <p>หมายเหตุ &nbsp;<%= track.dispNote %>
            </p>
          </div>
          <div class="align-right" style="display: inline-block;">
            <div class="w3-container-sign">
              <h6><b>ผู้รับเอกสาร (กรุณาเขียนตัวบรรจง)</b></h6>
              <hr>
            </div>
          </div> 
        </div>
      </div>
      <% }) %>
      <% } else { %>
        <p>No tracks found.</p>
      <% } %>
    </div>
    <footer class="w3-container w3 w3-center w3-margin-top noPrint">
      <button class="printButton noPrint" onclick="window.print();">Print</button>
      <a type="button" class="printButton noPrint" href="/">กดเมื่อเสร็จสิ้น</a>
    </footer>
</body>
</html>